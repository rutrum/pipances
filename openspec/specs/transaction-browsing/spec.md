## Purpose
~~Allow users to browse, filter, sort, and paginate through approved transactions.~~

**REMOVED** — All transaction browsing functionality has been absorbed into the Explore page (`/explore`) and the Data page (`/data/transactions`). See `explore/spec.md` and `data-page/spec.md`.
