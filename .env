# Pipances Configuration (development defaults)
# These values are relative to the project root (the working directory when running the app).

PIPANCES_DB_PATH=./pipances.db
PIPANCES_STATIC_DIR=./static
PIPANCES_IMPORTERS_DIR=./importers
PIPANCES_TEMP_DIR=/tmp/pipances_imports
